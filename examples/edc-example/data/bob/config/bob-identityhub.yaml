web.http:
  port: 7090
  path: "/api"
  
  # This contains both the Presentation & Storage API (Github Identity Hub API documentation),
  # now referred to as the Credentials API. Possibly also the same as the DCP API as mentioned 
  # in the Adopters manual.
  credentials:
    port: 7091
    # This is a deviation from the Data Ecosystems EDC demo configuration, which 
    # uses path `/api/resolution`.
    path: "/api/credentials"
  identity:
    port: 7092
    # This is a deviation from the Data Ecosystems EDC demo configuration, which 
    # uses path `/api/management`.
    path: "/api/identity" 
  did:
    port: 7093
    path: "/"
  sts:
    port: 7094
    path: "/api/v1/sts"
  jwks:
    port: 7095
    path: "/api/jwks"
  issueradmin:
    port: 7096
    path: "/api/issueradmin"
  issuance:
    port: 7097
    path: "/api/issuance"
  statuslist:
    port: 7098
    path: "/statuslist"

edc:
  web.rest.cors:
    enabled: true
    headers: "origin, content-type, accept, authorization, x-api-key"
    origins: "*"
    methods: "GET, POST, DELETE, PUT, OPTIONS, PATCH"

  iam.did.web.use.https: false

  # Can probably be removed as the accounts STS API has been removed
  api.accounts.key: "YaO5UMkYg2w586Cg017iy6Q7A6-EyuOt"

  issuer.statuslist.signing.key.alias: "bob-signing-key"

  ih:
    # This value can be removed once the official issuance implementation fully replaces the ad-hoc one.
    issuer.participant.id: "bob"
    api:
      participants:
        # This is used as the participant context ID within the Identity Hub.
        # It therefore is present in path of URLs to the API of this Hub
        # instance, base64 encoded.
        # Not the same as the participant ID (as assigned to the control plane),
        # which is a did:web.
        bob:
          name: "bob"
          did: "did:web:host-bob"
          active: true
          roles:  # Required but can be empty
          password: "$2a$10$KcjjniNy/p45X3yOWcDOY.lZ7ziNpA9pngh218lVMzdRqgYl0Wf06"
          private.key: LS0tLS1CRUdJTiBQUklWQVRFIEtFWS0tLS0tCk1DNENBUUF3QlFZREsyVndCQ0lFSUVMelhlUWRTeW9TaVVqaHFKMkVxUndKOTV0WGtLM1RYMG1DNnd1K21Ra1AKLS0tLS1FTkQgUFJJVkFURSBLRVktLS0tLQo=
          public.key: LS0tLS1CRUdJTiBQVUJMSUMgS0VZLS0tLS0KTUNvd0JRWURLMlZ3QXlFQWJqVlQ2Y1NiWEozTHhVY3hwQ0pVTklsUkJpMG15RDVUZ29xSC9NZ05GL1U9Ci0tLS0tRU5EIFBVQkxJQyBLRVktLS0tLQo=
          sts.secret: "1yj1R5jqQr1Zy1M7wg9y8uJjHsUQQH1LOp3oF0IFxeo="
          api.key.override: "Ym9i.c3VwZXItc2VjcmV0LWtleQo="
          credentials: "data/bob_vc.json" # Required!
          services:
            dsp:
              endpoint: "http://localhost:9083/api/dsp"
              id: "dsp"
              type: "DSPMessaging"
            credential:
              endpoint: "http://localhost:7091/api/resolution/v1/participants/Ym9i"
              id: "CredentialService"
              type: "CredentialService"
            issuerservice:
              endpoint: "http://localhost:7097/api/issuance/v1alpha/participants/Ym9i"
              id: "IssuerService"
              type: "IssuerService"
